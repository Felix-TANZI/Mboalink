image: node:20

stages:
  - test

cache:
  key:
    files:
      - package-lock.json
  paths:
    - .npm/

backend_tests:
  stage: test
  variables:
    NODE_ENV: test
    NPM_CONFIG_CACHE: "$CI_PROJECT_DIR/.npm"
  script:
    - npm ci
    - npm test
